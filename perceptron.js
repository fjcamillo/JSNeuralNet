const weights = {
    w1: ,
    w2: ,
}
const bias = {
    b1: 
}

export const perceptron(weights, bias, x) => {
    logit = weight*x + bias 
    softmax = 1 / 1 + math.e**-logit
    return softmax
}
